{% extends './_layouts/base.njk' %}
{% from './components/button/button.njk' import button %}

{% block title %}
  frōntensō-11ty-starter
{% endblock %}

{% block content %}
  <div class="page-home page bg-gray-900 text-gray-100 min-h-screen py-8">
    <div class="container mx-auto px-4">
      <div class="flex items-center mb-10">
        <svg class="mr-3 w-10 h-10 text-blue-400 fill-current">
          <use xlink:href="#icon-logo"></use>
        </svg>
        <h1 class="text-2xl font-medium">frontenso-11ty-starter</h1>
      </div>
      <h2 class="text-2xl font-medium mb-4">Core Features</h2>
      <ul
        class="mb-14 text-sm lg:text-base font-semibold bg-gray-800 border border-gray-700 rounded-lg p-6 max-w-xl list-disc list-inside space-y-2 text-gray-100 marker:text-blue-400"
      >
        <li>Component-based approach.</li>
        <li>Fast builds with Gulp and SCSS support.</li>
        <li>
          Modern image formats out of the box (AVIF, WebP) and image
          optimization.
        </li>
        <li>
          No JS-framework dependencies (you can add preact or any other
          framework though).
        </li>
        <li>Live reloading.</li>
        <li>Linters included.</li>
        <li>System notifications for build errors.</li>
        <li>Webpack config for most cases.</li>
        <li>SVG sprite generation and optimization.</li>
        <li>Environment variables support via .env files.</li>
        <li>HTML minification for production builds.</li>
        <li>Flexible static path configuration.</li>
        <li>(Optional) TailwindCSS with JIT.</li>
        <li>(Optional) TypeScript support using JSDoc notation.</li>
      </ul>

      <p class="mb-14 max-w-2xl">
        This is a production-ready component-based starter that features
        Nunjucks, SCSS, TailwindCSS (with JIT compiler), Webpack, ESNext, and
        live reloading.
      </p>
      <div class="mb-14">
        <p class="font-bold hover:opacity-80 text-lg">
          Please also read
          <a
            href="https://github.com/einomi/frontenso-11ty-starter"
            target="_blank"
            class="text-blue-400 underline"
          >
            the documentation on GitHub
          </a>
        </p>
      </div>
      <h3 class="text-lg font-semibold mb-4">
        A UI component example (check out the code)
      </h3>
      {{
        button({
          className: 'page-home__button',
          children: 'Buy something',
          chevronRight: true
        })
      }}
      {% include './components/some-component/some-component.njk' %}
      <section class="mt-14">
        <h3 class="text-lg font-semibold mb-5">
          Image example 1. AVIF and WebP are generated using
          {{ '{ % image % }' }} Nunjucks tag.
        </h3>
        {# not passing maxWidth here #}
        <div class="max-w-xs">
          {%
            image './src/images/cat.jpg',
            {
              alt: 'Cat',
              class: 'example-image block max-w-full h-auto'
            }
          %}
        </div>
      </section>
      <section class="mt-14">
        <h3 class="text-lg font-semibold mb-5">
          Image example 2. maxWidth=500 is passed. AVIF and WebP are also
          generated.
        </h3>
        {# passing maxWidth=500 here #}
        <div class="max-w-xs bg-gray-800 p-2 rounded">
          {%
            image './src/images/wolf.png',
            {
              alt: 'Wolf',
              class: 'example-image block max-w-full h-auto'
            },
            500
          %}
        </div>
      </section>
      <section class="mt-14">
        <h3 class="text-lg font-semibold mb-1 max-w-xl">
          An image inlined in CSS example (compress such images manually!). Not
          recommended for wide usage, see below.
        </h3>
        <h4 class="max-w-xl text-lg text-red-400 mb-6 font-bold tracking-wide">
          <span class="uppercase">Attention!</span> Please use this feature with
          caution as it may bloat the final CSS file. Inlining images could be a
          good approach if the file is quite small, in other cases prefer
          `&#123;% image %&#125;` tag.
        </h4>
        <div class="page-home__inline-image-example"></div>
      </section>
      <section class="mt-14">
        <h3 class="text-lg font-semibold mb-5">
          Vector image example (using SVG sprite)
        </h3>
        <svg class="fill-current text-blue-400 w-20 h-20">
          <use xlink:href="#icon-logo"></use>
        </svg>
      </section>
      <div class="flex items-center">
        <div></div>
        <div></div>
      </div>
      <section class="mt-14">
        <h3 class="text-lg font-semibold mb-5">Tailwind CSS Example</h3>
        <div class="px-4 py-4 rounded w-64 shadow-md bg-gray-800">
          <h3 class="bold mb-2 text-blue-400 hover:opacity-80 font-bold">
            Some section
          </h3>
          <div class="flex">
            <svg
              class="relative top-0.75 mr-2 w-5 h-5 fill-current text-gray-100"
            >
              <use xlink:href="#icon-movies"></use>
            </svg>
            <div class="text-sm">
              <div class="line -mb-0.75">Movies</div>
              <div class="text-gray-400">For cosy evenings</div>
            </div>
          </div>
        </div>
      </section>
    </div>
  </div>
{% endblock %}
